# 작업 현황 인수인계 — 2026-08-26 (발표 당일, S6 파싱 버그 수정 확인 — 2차 재실행 필요)

> **다음 세션은 이 문서부터 읽을 것.** 앞 세션 문서는 [status-2026-08-25.md](status-2026-08-25.md),
> 계획 전체는 [plan-2026-08-26.md](plan-2026-08-26.md).
>
> **오늘이 발표일이다.** status-2026-08-25.md는 "다음 세션 할 일 = S6(Llama-3.1-8B)
> head 탐색"으로 지시했으나, 세션 시작 시점에 확인해보니 **head 탐색은 이미 끝나 있었다**
> (`results/2026-08-25_s6_llama8b/heads_agentdojo.json`, 커밋 `26fa7a3`). 코드 수정 없음.
>
> **S6 eval(Track B) 1차 실행 결과가 무효했다** — tool_call 파싱 성공률 **0%**(90/90 전부
> `no_tag`). 원인은 Qwen 전용 `<tool_call>` 태그 파서가 family 무관하게 하드코딩돼 있었던
> 것(Llama는 태그 없는 JSON, 키도 `arguments`가 아니라 `parameters`). **커밋 `bf78653`로
> 수정**했고, **서버에서 그 커밋으로 2차 재실행까지 이미 완료됨** — 파싱 성공률 0%→79.1%로
> 개선 확인(`results/2026-08-25_s6_llama8b/agentdojo_eval.json`, `env.git.commit=bf78653`).
>
> **그런데 2차 결과를 검토하다 파서에 2차(경미한) 버그를 하나 더 발견해 즉시 수정함**
> (커밋 `bd19216`) — "completion 전체가 `}`로 끝나야 완결"이라는 판정이 너무 엄격해서,
> JSON 뒤에 자연어 설명이 붙는 정상 응답(Llama에서 흔함)까지 `truncated`로 오분류하고
> 파싱을 시도조차 안 하고 있었다. **이 수정은 아직 서버에서 검증 전 — bd19216으로 3차
> 재실행이 필요하다.** 상세는 아래 5절.
>
> **⚠️ 다음 세션 바로 할 일**: `git pull` 후 커밋이 `bd19216`(또는 그 이후)인지 확인하고
> S6 eval 3차 재실행 (5.5절 커맨드). **3차 재실행 전까지는 지금 있는 79.1% 수치도 완전히
> 신뢰하지 말 것** — json_errors 22건 중 다수가 이 2차 버그로 인한 오분류일 가능성이 높다.
> 지금까지 확립된 32B 결과(S4/S5/tool_knowledge)만으로도 발표는 성립한다
> ([status-2026-08-24.md](status-2026-08-24.md) 6절) — 3차 재실행이 발표 전에 안 끝나면
> S6은 "패밀리 축 검증 시도 — 파싱 버그 2건 발견·수정, 최종 검증 전"이라는 **한계/후속
> 과제**로만 언급할 것.

---

## 1. 정정 — HF 게이트 승인이 이미 났다

status-2026-08-25.md 4절은 "`meta-llama/Llama-3.1-8B-Instruct`가 게이트 승인 대기 중이라
`unsloth/Meta-Llama-3.1-8B-Instruct`(게이트 없는 재업로드)로 우회"를 전제했다.

실제로는 `results/2026-08-25_s6_llama8b/heads_agentdojo.json`의 `env.argv`에
`--model meta-llama/Llama-3.1-8B-Instruct`(**정식 게이트 모델**, unsloth 아님)가 그대로
찍혀 있다 — 그 사이 승인이 났고 우회 없이 정식 모델로 실행됐다. **발표에 "unsloth
재업로드 사용" caveat을 넣을 필요 없음.**

## 2. S6 head 탐색(discover) 결과 — `results/2026-08-25_s6_llama8b/heads_agentdojo.json`

- 모델: `meta-llama/Llama-3.1-8B-Instruct`, `--family llama`, `--device_map auto`,
  `--head_n 200 --max_seq_len 1200 --batch_size 5`, dtype bf16, 커밋 `5905a1b`(dirty:false)
- `n_examples_used=149`, `n_oom_skipped=0`, `n_nan_skipped=0` — OOM/NaN 없이 전부 성공
- suite_counts: banking 57 / slack 50 / travel 13 / workspace 29 (ok)
- topk=20 heads 산출 완료

### 2.1 ⚠️ split_info — travel·workspace가 discovery 풀 안에서 quota를 못 채움

| suite | quota | head_pairs | shortfall | 탐색 풀 내 eval_pairs |
|---|---|---|---|---|
| banking | 50 | 57 | 0 | 7 |
| slack | 50 | 50 | 0 | 24 |
| **travel** | 50 | **13** | **37** | **0** |
| **workspace** | 50 | **29** | **21** | **0** |

`n_head_pairs=149, n_eval_pairs=31, shortfall_total=58` (전체 후보 `n_all_pairs=180`).

travel·workspace는 discovery 풀 안의 user_task 그룹이 head 탐색에 전부 소진돼, 탐색 풀
기준으로는 eval_pairs가 0이다. status-2026-08-25.md 3절이 "32B에서는(shortfall=3) 우연히
안 걸렸지만 다른 head_n/quota 조합에서 재발할 수 있다"고 경고했던 `pair_sampling.py`의
근본 이슈(미해결, 코드 수정 안 함)가 Llama-8B에서 **더 큰 폭으로(shortfall 37·21)
재발했다.**

**다만 이게 실제 eval 스킵을 의미하는지는 아직 확인 안 됨** — `run_agentdojo_eval.py`의
실제 heldout 후보는 discovery 풀이 아니라 **suite 전체**(`user_tasks × injection_tasks`)에서
head_groups만 제외하는 방식이라, 32B 때도 비슷한 shortfall(travel=3)에서 실제로는
`n_candidates=28, n_evaluated=15`로 정상 평가됐다(status-2026-08-25.md 1.5절). Llama-8B도
실제로 eval을 돌려봐야 travel/workspace가 진짜 0쌍인지 확인 가능 — **아래 3절 실행 후
`n_candidates`/`n_evaluated`를 반드시 확인할 것.**

## 3. 다음 태스크 — S6 eval(Track B) 실행

32B S5와 동일한 파이프라인, 코드 수정 0.

```bash
python run_agentdojo_eval.py \
  --model meta-llama/Llama-3.1-8B-Instruct --family llama \
  --device cuda:0 \
  --heads_json results/2026-08-25_s6_llama8b/heads_agentdojo.json \
  --eval_split heldout --limit_pairs 15 \
  --out_json results/2026-08-25_s6_llama8b/agentdojo_eval.json
```

- fp16 ~16GB — Titan RTX 24GB 한 장에 그대로 들어감. `--four_bit`, `--device_map auto`
  불필요(8B는 32B와 달리 분산 안 씀)
- `--attack important_instructions`가 기본값 → 32B 1번째 eval과 대응. 여유 있으면
  32B 2절처럼 `--attack tool_knowledge` 비교도 추가 가능(선택)
- 실행 후 로그의 `n_candidates`/`n_evaluated` per suite 확인 → travel/workspace가 0으로
  나오면 "S6은 banking/slack만 검증됨"으로 발표 문구 조정 필요 (2.1절)

---

## 4. 미해결 항목 갱신 (`status-2026-08-25.md` 3절 기준, 변경분만)

| 항목 | 상태 |
|---|---|
| HF 게이트(Llama-3.1-8B) 수동 리뷰 대기 | **해소됨** — 정식 모델로 이미 실행 완료 (1절) |
| `pair_sampling.py` eval 최소 보장 안 됨 | **재발 확인**. Llama-8B travel/workspace shortfall 37·21 (2.1절) — 다만 실제 heldout 후보는 shortfall과 무관하게 travel 112/workspace 448로 정상 산출됨(5절 `eval_split`). 코드 수정은 여전히 안 함 |
| ~~S6 eval 실행~~ | **실행 완료, 그러나 결과 무효** — tool-call 파싱 0% 성공. 신규 블로커, 상세는 5절 |

---

## 5. S6 eval(Track B) 실행 결과 — `results/2026-08-25_s6_llama8b/agentdojo_eval.json`

3절 커맨드 그대로 실행됨 (`--attack important_instructions`, `--limit_pairs 15`,
커밋 `87b7d8a`, dtype bf16). **수치 자체는 나왔지만 무효하다** — 아래가 그 진단.

### 5.1 tool_call 파싱 성공률 0% — Qwen 전용 포맷을 family 무관하게 하드코딩한 버그

```
parse_stats: n_calls=90  ok=0  no_tag=90  truncated=0  json_errors=0
```

90/90(100%) `no_tag` — Qwen 32B/7B에서 봤던 "가끔 실패"(29~32%)가 아니라 **단 한 번도
성공 못 함**. 모든 row에서 `k0_utility==kN_utility`, `k0_security==kN_security`가 완전히
같다(예: banking 15쌍 전부) — knockout을 켜든 끄든 모델이 tool을 아예 안 쓰니 차이가
날 수가 없다. **즉 `kN_security_rate=0.0`은 방어가 작동한 게 아니라 애초에 공격이 시도될
기회(tool 호출)가 없었다는 뜻이다.**

**원인**: `adapters/agentdojo_pipeline.py:36`
```python
_TOOL_CALL_RE = re.compile(r"<tool_call>\s*(\{.*?\})\s*</tool_call>", re.DOTALL)
```
이 태그가 **family 무관하게 하드코딩**돼 있다. 이 관례는 애초에 **Qwen2.5 전용**이다
— Qwen2.5 tokenizer의 `chat_template`(Jinja) 자체가 "함수 호출은 `<tool_call>` XML
태그로 감싸라"는 지시문을 템플릿 안에 박아넣고 있다(어댑터 파일 상단 docstring의
"Qwen2.5는 `<tool_call>` 포맷으로 파인튜닝돼 있어 그 포맷을 그대로 쓰는 게 안정적"이라는
근거가 바로 이거다). `_render_prompt`는 `self.tok.apply_chat_template(...)`을 쓰는데
S6에서는 `self.tok`가 **Llama 토크나이저**라서 Llama 자체 템플릿이 렌더링되고, **Llama
3.1 공식 템플릿은 이 `<tool_call>` 관례를 쓰지 않는다** — 모델에게 애초에 그 포맷으로
답하라고 지시한 적이 없는데 파서만 그 포맷을 기다리고 있었다.

`--family llama`는 지금까지 `attn_relevance.py`(LRP relevance 계산)에만 연결돼 있었고
tool-call 파싱 쪽엔 한 번도 연결된 적이 없었다 — S6 전까지는 전부 Qwen 계열만 돌려서
이 gap이 드러나지 않았을 뿐이다.

### 5.2 부수 이상 현상

- **travel: `per_suite.travel.n_pairs=0`인데 `eval_split.travel.n_evaluated=15`** — 15쌍이
  후보로 뽑혔는데 전부 `run_agentdojo_eval.py`의 `try/except`에서 예외로 죽어서 건너뛰어짐
  (콘솔에 `ERROR (...), skipping`만 찍히고 결과 JSON엔 안 남는 구조). 원인은 이 JSON만으론
  알 수 없다 — 서버 stdout 로그가 남아있으면 확인 필요. 5.1의 파싱 버그와 같은 원인인지
  별개 문제인지 미확인.
- **banking utility 46.7%(7/15)**가 tool을 한 번도 안 썼는데도 나왔다 — AgentDojo banking
  suite에 tool 실행 없이 텍스트 답변만으로 채점되는 read-only형 task가 섞여 있어서일
  가능성이 높다(우연히 맞은 것이지 방어 효과 아님). slack/workspace는 utility도 0%.

### 5.3 결론 (당시) — 발표에 쓰면 안 됨 / 이후 코드 수정으로 해소 시도

**당시 판단**: S6 utility/security 수치는 "Llama에서도 knockout이 통한다/안 통한다"의
증거로 쓸 수 없었다. 지금까지 확립된 32B 결과(S4/S5/tool_knowledge)만으로 발표는 성립함
(status-2026-08-24.md 6절).

**같은 세션 안에서 5.4로 진단 → 코드 수정까지 진행함.** 아래 5.4 참고 — 아직 서버
재실행 검증 전이라 "발표에 쓰면 안 된다"는 결론 자체는 **재검증 전까지 유효**하다.

### 5.4 원인 진단 + 코드 수정 완료 (2026-08-26, 같은 세션)

**실측**: `AutoTokenizer.from_pretrained("meta-llama/Llama-3.1-8B-Instruct").apply_chat_template(
messages, tools=tools, add_generation_prompt=True, tokenize=False)`를 서버에서 직접 찍어봄
(GPU 불필요, 이미 캐시된 tokenizer만 필요). 결과: Llama 3.1 공식 chat_template이 시스템
프롬프트에 다음을 명시적으로 지시함 —

> "Respond in the format {"name": function name, "parameters": dictionary of argument
> name and its value}."

즉 **태그 없이 단일 JSON**, 그리고 **키 이름이 `arguments`가 아니라 `parameters`** —
5.1의 `_TOOL_CALL_RE`(Qwen 전용 `<tool_call>` 태그)가 이 포맷을 원천적으로 인식 못 했던
것과 정확히 일치. Qwen 쪽은 이 태그 관례를 Qwen 자신의 chat_template이 지시하는 것이었고,
Llama는 자기 방식대로 정상 응답했을 뿐인데 파서가 Qwen 방식만 인식했다는 뜻.

**수정** (todo.md P13, 코드는 `adapters/agentdojo_pipeline.py` / `run_agentdojo_eval.py`):
- `_parse_tool_calls_qwen` / `_parse_tool_calls_llama`로 파서를 family별로 분리
- Llama 쪽은 마크다운 코드펜스 제거 → 첫 `{`~마지막 `}` 추출 → `parameters` 키로 조회
- `KnockoutLocalLLM(family=...)` 신규 파라미터, `run_agentdojo_eval.py`가 `args.family`를
  그대로 전달 (기존엔 `--family`가 `attn_relevance.py`의 LRP 계산에만 연결돼 있었음)
- family를 하드코딩된 Qwen 포맷으로 강제 통일하는 대신 **각 모델의 native 포맷을 그대로
  인정**하는 방향 선택 — agentdojo 기본 포맷을 1.5B에 강제했을 때도 안 먹혔던 전례가 있어,
  파인튜닝 습관과 다른 포맷을 강제 지시하는 쪽이 더 불안정할 위험이 크다고 판단
- 로컬 Python으로 구문 검증만 완료(`py_compile` 통과), agentdojo 등 의존성이 로컬에 없어
  실제 import/실행 테스트는 서버에서만 가능

**⚠️ 다음 세션 바로 할 일 — 서버에서 S6 eval 재실행**:
```bash
python run_agentdojo_eval.py \
  --model meta-llama/Llama-3.1-8B-Instruct --family llama \
  --device cuda:0 \
  --heads_json results/2026-08-25_s6_llama8b/heads_agentdojo.json \
  --eval_split heldout --limit_pairs 15 \
  --out_json results/2026-08-25_s6_llama8b/agentdojo_eval.json
```
`parse_stats.ok`가 Qwen 기준 정상 범위(68~71%)로 돌아오는지 먼저 확인 — 그래야 utility/
security 수치를 발표에 쓸 수 있다. 재확인 없이 새 수치를 그대로 쓰지 말 것.

### 5.5 2차 재실행 결과 — 파싱은 크게 개선됐지만 2차(경미한) 버그 하나 더 발견·수정

서버에서 위 커맨드를 커밋 `bf78653`로 이미 재실행함(`env.timestamp=2026-08-26T02:21:29Z`,
`env.git.dirty=false`). 결과를 이 세션에서 받아 확인함.

**파싱 성공률 0% → 79.1%로 개선 확인**:
```
parse_stats: n_calls=311  ok=246(79.1%)  no_tag=36(11.6%)  truncated=7(2.3%)  json_errors=22(7.1%)
```
Qwen 기준선(68~71%)보다 오히려 **더 높다** — 파싱 자체는 정상 작동한다는 뜻.

**처음으로 진짜 knockout 신호가 나타남**: slack suite `k0_security=13.3%(2/15) →
kN_security=0%(0/15)` — 공격 2건이 knockout 후 둘 다 실패로 전환. utility는 두 조건에서
동일(banking 0.4/0.4, slack 0.533/0.533)하게 유지 — 32B/7B에서 봤던 패턴(공격 억제 +
정상 기능 보존)과 방향이 일치하는 **첫 유효 Llama 결과.**

**그런데 `json_errors=22`(7.1%)가 예상보다 높아 `truncated_examples`(5건)를 직접 검토함.**
전부 이런 형태였다:
```
The function call {"name": "get_most_recent_transactions", "parameters": {"n": "1"}}
returns the most recent transaction, ... The amount of this transaction is 10.0.
```
이건 **완결된 유효 JSON 뒤에 모델이 자연어 설명을 덧붙인 것**뿐인데, 5.4에서 짠
`has_close = completion.strip().endswith("}")` 조건이 "문자열 전체가 `}`로 끝나야
완결"로 너무 엄격하게 판정해서 — 유효한 tool call인데도 `truncated`로 잘못 분류되고
**파싱 시도조차 안 하고 버려지고 있었다.** Qwen 원본 로직(닫는 태그 유무)을 그대로
흉내내려다, Llama는 태그가 없다는 차이를 놓친 것.

**수정 (커밋 `bd19216`)**: 판정 기준을 "completion 전체 끝맺음"이 아니라 **추출한 JSON
스팬의 중괄호 개수 균형**으로 바꿈 — `_extract_json_object`가 첫 `{`~마지막 `}` 사이를
뽑으므로, 진짜로 `max_new_tokens`에 잘렸으면 중괄호 개수가 안 맞을 가능성이 높고, 뒤에
붙는 자연어 설명 자체는 중괄호를 안 쓰므로 이 판정에 영향이 없다. 실제 `truncated_examples`
5건으로 로컬 검증(의존성 없는 순수 함수 재현) — **전부 정상 파싱됨**을 확인.

**⚠️ 3차 재실행 필요**: 이 수정은 아직 서버에서 실행 검증 전이다. `json_errors` 22건 중
몇 건이 이 버그로 인한 오분류였는지(즉 3차 재실행 후 `ok`가 79.1%에서 얼마나 더 오르는지)
확인해야 최종 수치를 신뢰할 수 있다. 특히 이번에 나온 slack security 신호(13.3%→0%)가
3차에서도 유지되는지가 핵심 확인 사항 — n이 작아(15쌍) 몇 건의 재분류만으로도 비율이
바뀔 수 있다.
