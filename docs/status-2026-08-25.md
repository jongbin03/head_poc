# 작업 현황 인수인계 — 2026-08-25 (32B S5 eval 완료)

> **다음 세션은 이 문서부터 읽을 것.** 앞 세션 문서는 [status-2026-08-24.md](status-2026-08-24.md),
> 계획 전체는 [plan-2026-08-26.md](plan-2026-08-26.md).
>
> **발표까지 하루 남았다 (8/26).** 32B는 S4·S5·tool_knowledge 비교 eval까지 전부 끝났다 —
> 더 돌릴 필요는 없어 보인다.
> **다음 세션 바로 할 일**: 아래 4절 — Llama-3.1-8B 패밀리 축(S6), 코드 수정 0.
> ⚠️ 정식 `meta-llama/Llama-3.1-8B-Instruct`는 HF 게이트 승인 대기 중(수동 리뷰) — 4절의
> `unsloth/Meta-Llama-3.1-8B-Instruct`(게이트 없는 동일 가중치 재업로드)로 우회 가능.
> 시간이 빠듯하면 S6 없이 지금 수치로도 발표는 성립한다 ([status-2026-08-24.md](status-2026-08-24.md) 6절).

---

## 1. 32B S5(eval) 결과 — `results/2026-08-24_s4_32b/agentdojo_eval.json`

서버에서 실행 완료된 결과를 pull해서 확인함 (커밋 `53f4754` 기준, dirty:false — `device_map`
수정 `b8637dd` 반영된 상태로 정상 실행됨). n=57 pairs, 20 heads, `--eval_split heldout`.

### 1.1 전체 수치

| | k=0 (baseline) | k=N (knockout) | 변화 |
|---|---|---|---|
| utility | 45.6% | 47.4% | +1.8pp |
| security(=공격 성공률) | 8.77% | 3.51% | -5.3pp (약 60% 감소) |

### 1.2 suite별 — 전체 수치가 slack 하나에 좌우된다

| suite | k0 util | kN util | k0 attack | kN attack |
|---|---|---|---|---|
| banking | 53.3% | 73.3% | 0% | 0% |
| slack | 53.3% | 53.3% | **33.3%** | **13.3%** |
| travel | 20.0% | **6.7%** | 0% | 0% |
| workspace | 58.3% | 58.3% | 0% | 0% |

### 1.3 발표 문구 수정이 필요한 두 지점

- **"baseline 공격 성공률 floor 2% 안팎, 모델 스케일 무관"** (`status-2026-08-24.md` 5절) —
  32B에서는 안 맞는다. banking/travel/workspace는 baseline부터 0%지만 **slack만 33%**로
  전체 평균(8.77%)을 끌어올린다. "2% floor"를 그대로 쓰면 이 결과와 모순되므로, suite별
  편차가 크다는 caveat을 붙이거나 문구를 뺄 것.
- **"knockout이 공격을 완전 억제한다"** (7B에서 3번 반복된 1/1 패턴) — 32B에서는 재현 안
  됨. slack 5건 중 2건은 knockout 후에도 공격이 성공(부분 억제, 60% 감소이지 100%가 아님).
  "억제하지만 완전하진 않다"로 조정할 것.

### 1.4 표본이 작아 캐비엇이 필요한 관측 (단정하지 말 것)

- **travel utility가 knockout으로 오히려 하락** (20%→6.7%, 3건 중 2건이 knockout 후 실패).
  n=15로 매우 작고, travel head 자체가 S4에서 OOM 편향(2026-08-24 기록, 성공 표본 13개뿐)으로
  뽑힌 것이라 head 신뢰도 자체가 낮다. "knockout이 travel에 해롭다"는 결론은 근거 부족.
- **banking utility가 knockout으로 20pp 상승** (53.3%→73.3%) — n=15라 우연일 가능성 배제 못함.

### 1.5 부수적으로 확인된 좋은 소식 — travel eval이 이번엔 0건이 아니었다

`status-2026-08-24.md` 2.2/2.3/5절이 우려했던 "S4에서 travel head_pairs가 quota(50)를
못 채워(47개, shortfall=3) `--eval_split heldout`에서 travel 전체가 스킵된다"는 문제가 이번
S5 실행에서는 **발생하지 않음** — `eval_split.travel.n_candidates=28, n_evaluated=15`로 정상
평가됨. head 탐색(S4)용 quota 부족과 eval(S5)용 heldout 후보 풀은 별개 경로라 S4의 shortfall이
S5까지 전파되지 않은 것으로 보인다. **`pair_sampling.py`의 근본 이슈(미해결 항목, 아래 2절)
자체는 여전히 남아 있다** — 이번엔 우연히 안 걸린 것뿐이니 다른 조건(head_n/quota 조합)에서
재발할 수 있다는 점은 유지.

### 1.6 tool_call 파싱 실패율 — 여전히 높음

`n_calls=364, ok=249(68.4%), no_tag=92(25.3%), truncated=23(6.3%), json_errors=0`.
실패율 31.6% — 8/19의 22.2%보다 높음. `max_new_tokens=128` 상향 검토는 미착수 (아래 2절 유지).

---

## 2. tool_knowledge 공격 비교 eval — `results/2026-08-24_s4_32b/agentdojo_eval_toolknowledge.json`

같은 heldout split·같은 `heads_agentdojo.json`, `--attack tool_knowledge`(공격 tool·인자까지
문구에 박아 넣어 추론 부담을 없앤 버전)로 재실행. n=55 pairs(important_instructions는 57 —
`ToolKnowledgeAttack`이 placeholder_args 없는 case를 스킵해서 약간 다름).

### 2.1 전체 수치 비교

| | important_instructions (n=57) | tool_knowledge (n=55) |
|---|---|---|
| k0 utility | 45.6% | 54.5% |
| kN utility | 47.4% | 56.4% |
| k0 security(공격 성공률) | 8.77% | **14.5%** |
| kN security | 3.51% | **9.1%** |

### 2.2 핵심 발견 — "공격 문구는 성공률에 영향 없다"는 7B 결론이 32B에서 깨짐

`docs/todo.md` 2026-07-31 기록: 7B에서 같은 비교(important_instructions vs tool_knowledge)를
했을 때 성공률이 거의 그대로였음(0.021→0.022). **32B에서는 baseline 공격 성공률이
8.77%→14.5%로 거의 2배 뜀.** 이 결론을 그대로 발표에 쓰면 안 되고, "7B에서는 공격 문구가
무관했지만 32B에서는 유의미하게 영향을 준다"는 스케일-의존적 차이로 정정할 것.

### 2.3 suite별 — slack 집중 현상이 두 번째 공격으로도 재확인됨

| suite | important: k0→kN | tool_knowledge: k0→kN |
|---|---|---|
| banking | 0% → 0% | 0% → 0% |
| **slack** | **33.3% → 13.3%** | **53.3% → 33.3%** |
| travel | 0% → 0% | 0% → 0% |
| workspace | 0% → 0% | 0% → 0% |

**두 공격 다 성공 사례가 전부 slack에서만 나온다.** banking/travel/workspace는 공격 종류와
무관하게 0% — 1.3절의 "2% floor는 slack 하나가 끌어올린 것"이라는 caveat이 우연이 아니라
suite 구조 자체의 문제라는 근거가 하나 더 생김.

### 2.4 knockout 효과는 유지되지만, 공격이 강할수록 절대 잔여 위험이 커짐

- important_instructions: slack 33.3%→13.3% (**60% 상대 감소**)
- tool_knowledge: slack 53.3%→33.3% (**37.5% 상대 감소**)

knockout이 두 공격 다 방향은 맞게 줄이지만(상대 감소율 자체는 비슷한 오더), **공격이 강할수록
knockout 후에도 남는 절대적 공격 성공률이 커진다**(33.3%는 방어 후에도 상당히 높음). "knockout이
공격을 억제한다"는 주장은 유지되나 "얼마나 세게 억제하는지는 공격 강도에 따라 다르다"는 caveat
필수. utility는 두 공격 다 knockout 후 오히려 소폭 상승 — 정상 과업 훼손 없음은 일관됨.

### 2.5 parse_stats

`n_calls=372, ok=264(71.0%), no_tag=101, truncated=7, json_errors=0` — 실패율 29.0%.
important_instructions(31.6%)와 비슷한 수준으로 여전히 높음 (아래 3절 미해결 항목 유지).

---

## 3. 미해결 항목 갱신 (`status-2026-08-24.md` 5절 기준)

| 항목 | 상태 |
|---|---|
| tool_call 파싱 실패 31.8%→31.6%(→29.0%, tool_knowledge) | **유지**. `max_new_tokens` 상향 검토 안 함 |
| baseline 공격 성공률 floor ~2% | **32B에서 반박됨** (1.3절), **공격 문구 무관하다는 7B 결론도 32B에서 반박됨** (2.2절) — suite별 편차 큼(slack만 유효), 표본 57~55쌍으로는 최종 결론 내리기엔 이름 |
| `pair_sampling.py` eval 최소 보장 안 됨 | **유지, 이번엔 미발생** (1.5절) — 코드 수정은 여전히 안 함 |
| 32B S4 travel/workspace OOM 편향 | **유지** — S5 head 자체가 이 편향의 영향을 받은 채로 쓰임 (1.4절 travel 캐비엇의 근본 원인) |
| HF 게이트(Llama-3.1-8B) 수동 리뷰 대기 | **신규**. `unsloth/Meta-Llama-3.1-8B-Instruct`(게이트 없는 재업로드)로 우회 가능, 4절 참고 |
| 오래된 주석(P2-d 누수 표현) | 유지, 낮은 우선순위 |
| 태스크 D (`head_pairs` suite 블록 순서) | 유지, 낮은 우선순위 |

---

## 4. 다음 세션에서 할 것 — Llama-3.1-8B 패밀리 축 (S6)

`status-2026-08-24.md` 2.4절 그대로 유효. 요약:

- **모델**: `meta-llama/Llama-3.1-8B-Instruct` — HF 게이트 모델, 라이선스 동의 필요. **2026-08-25
  기준 승인 대기 중**("awaiting a review from the repo authors", 수동 리뷰라 시간 예측 불가).
  발표 전까지 안 나면 게이트 없는 동일 가중치 재업로드 `unsloth/Meta-Llama-3.1-8B-Instruct`
  (`--model`만 교체, `--family llama`는 동일 — architecture가 같은 `LlamaForCausalLM`)로 대체.
  이 경우 결과 기록에 "unsloth 재업로드 사용(게이트 대기 우회)" caveat 남길 것.
- fp16 ~16GB, 기존 Titan RTX 24GB 카드 1장에 그대로 들어감. 4bit·`--device_map` 불필요
- **코드 수정 0** — `--family llama` 이미 구현됨
- Qwen2.5-7B 4bit 조건(`head_n=200, max_seq_len=2000, batch_size=5`)을 fp16 Llama에 그대로
  못 옮김(활성화 메모리 프로파일 다름) — 32B 때처럼 **먼저 소규모 스모크로 OOM 여부 확인** 후
  본 실행 수치 결정
- S4(discover) → S5(eval), `--eval_split heldout` 연결까지 32B와 동일 파이프라인 재사용

발표까지 시간이 빠듯하면 S6 없이 지금까지 수치(지시 1·2·4 충족, `status-2026-08-24.md` 6절)로도
발표는 성립한다 — 무리해서 S6까지 욱여넣지 말 것.
